# Fixtures

Fixture files are only for parser and pipeline development.

Use them with:

```bash
OPENMEVZUAT_FIXTURE_MODE=true clojure -M:openmevzuat build
```

Do not treat fixture-derived canonical files as official legal text.

